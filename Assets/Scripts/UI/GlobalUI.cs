using UnityEngine;

public class GlobalUI : MonoBehaviour
{
    private static GlobalUI instance;

    void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}